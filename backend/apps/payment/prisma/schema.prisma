generator client {
  provider = "prisma-client-js"
  output   = "../../../node_modules/.prisma/payment-client"
}

datasource db {
  provider = "postgresql"
}

model Payment {
  id              String        @id @default(uuid())
  sequenceId      Int           @unique @default(autoincrement()) @map("sequence_id")
  orderId         String        @db.VarChar(36) @unique @map("order_id")
  idempotencyKey  String?       @db.VarChar(36) @unique @map("idempotency_key")
  externalId      String?       @db.VarChar(255) @unique @map("external_id")
  preferenceId    String?       @db.VarChar(255) @unique @map("preference_id")
  amount          Float
  status          PaymentStatus @default(PENDING)
  gatewayResponse Json?        @map("gateway_response")
  createdAt       DateTime      @default(now()) @map("created_at")
  updatedAt       DateTime      @updatedAt @map("updated_at")

  @@map("payments")
}

enum PaymentStatus {
  PENDING
  APPROVED
  REJECTED
  CANCELLED
  REFUNDED
}
